import Login from "../../features/authentication/Login";

const LoginPage = () => {
  return (
    <main className="w-full h-screen flex flex-center gradient">
      <Login />
    </main>
  );
};

export default LoginPage;
